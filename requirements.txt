# ── Core framework ──
Django>=5.1,<5.2

# ── Configuration / environment ──
django-environ>=0.11,<0.12

# ── PostgreSQL driver (psycopg 3) ──
psycopg[binary]>=3.1,<3.3

# ── WSGI server (run behind Nginx via systemd) ──
gunicorn>=22.0,<23.0

# ── Static files served through WhiteNoise (fallback if not using Nginx alias) ──
whitenoise>=6.6,<7.0

# ── Redis client for the shared cache (used when REDIS_URL is set) ──
redis>=5.0,<6.0

# ── Reporting ──
weasyprint>=62.0          # HTML → PDF utilisation reports
openpyxl>=3.1,<3.2        # Excel export

# ── Optional: LDAP / Omhare directory integration (enable in accounts/backends.py) ──
# django-auth-ldap>=4.8,<5.0
